# Atlas Data Directory

- `canon-manifest.json` – curated configuration for realms, deities, luminors, and open-source policy.
- `canon-sources.json` – generated file capturing canonical markdown contents (run `npm run generate:data`).
