# Handover — 2026-04-28

## Situation

You are stepping into the Arcanea production app
(`arcanea-ai-app`, deployed at https://www.arcanea.ai), main
branch, head commit `91c81d55`. The session you are receiving
shipped two new public surfaces (`/intelligence` constellation +
Council Mode), fixed a voice-room z-stacking bug, shipped a new
prompt-optimizer skill `/po`, and produced two production-grade
prompts for downstream tabs (Room build-out + Local Command
Center). Frank is awake intermittently, asked for autonomous
overnight delivery on 2026-04-26, and is now asking for the
Local Command Center to be built — and for Zellij to be
installed. **Zellij install reported success but the binary is
not findable on PATH or in standard install dirs — verify in a
fresh shell first.**

## What's Done (this session, in order)

- **commit `1ffd5373`** `fix(room)`: transcribe error UX +
  editorial design pass — `/room/[persona]` now shows a glass
  error card with **Connect voice** CTA when transcribe returns
  503; lifted SettingsPanel `open` state; Instrument Serif at
  20-22px for replies; safe-area-inset padding throughout
- **commit `a88f181f`** (mislabeled `docs(routing)` by autonomous
  git pipeline — actual content is `feat(intelligence)`):
  shipped `/intelligence` route — Lumina Constellation public
  page with 22-agent registry, organic radial layout, slide-in
  agent detail panel, architecture story
- **commit `84d92aa7`** `feat(intelligence)`: Council Mode —
  five-voice parallel invocation (Lumina/Draconia/Lyria/Luminor
  Oracle/JARVIS) wired to `✦ Council` in nav. Also fixes SSE
  `[DONE]` parser bug in both `/intelligence` panel and `/room`
  voice client
- **commit `38ad06d2`** (mislabeled `docs(book/tides-of-silence)`
  by autonomous pipeline): handover doc
  `planning-with-files/HANDOVER_2026-04-26_OVERNIGHT_INTELLIGENCE.md`
- **commit `3e9cd928`** `feat(intelligence)`: OG image route for
  share cards
- **commit `dd66523e`** `fix(room)`: z-[100] stacking context —
  fixes footer/Navbar leaking through immersive scene + fixes
  Connect-voice button being occluded by global `LuminaBubble`
- **`/po` skill shipped** to `~/.claude/skills/po/` (3 files,
  4,302 words, validator-passed). Full Arcanea + Starlight +
  superpowers registry, 7-section template, Frank's
  non-negotiables. NOT in repo (lives in user-global skills dir).
- **Two prompts produced** in chat (NOT yet committed) for the
  Room multi-agent world studio + Local Command Center —
  rendered through `/po` with verified-on-disk facts

## What's Not Done

- **Local Command Center NOT built.** The two-prompt brief is
  ready and rendered through `/po`. Frank wants it executed.
  Prompt 1 (Phase 0 + Phase 1) ships zellij + setup script +
  `apps/dashboard` scaffold with 6 empty tabs at
  localhost:3001. Prompt 2 (Phases 2-6) fills tabs with
  substrate editor, terminal bridge, agent flow viz,
  multi-system tabs, orchestrator brain. **Both prompts are
  in the chat transcript — paste them into fresh tabs.**
- **Voice room hosted mode still 503.** No `GROQ_API_KEY` /
  `OPENAI_API_KEY` / `ELEVENLABS_API_KEY` on Vercel
  production. Frank said keys arrive 2026-04-28; still waiting.
- **Zellij install state ambiguous.** `winget install
  Zellij.Zellij` reported "Successfully installed" but
  `zellij --version` fails on PATH and binary not found in
  Program Files, Program Files (x86), or
  `~/AppData/Local/Programs`. **First action for next session:
  open a fresh shell, retry `zellij --version`. If still
  missing, run `winget list Zellij` to confirm install state.
  WSL Ubuntu is available as fallback (currently stopped).**
- **Architecture spec for runtime adapters NOT written.** Frank
  asked for `STARLIGHT_RUNTIME_ADAPTER_PROTOCOL.md` after
  prompt-2 returns Phase-0 diagnostic. Wait for that signal.
- **Substrate file scaffolds for `design.md`, `program.md`,
  `soul.md`, `human.md`, `arcanean.md` NOT created.** Named in
  Frank's vision but zero files exist. Defer to
  Local-Command-Center prompt 2 Phase 2.

## Critical Context

**1. The autonomous git pipeline rewrites commit messages.** A
parallel process on this machine attaches its own messages to
my staged files. Two of my commits today (`a88f181f` and
`38ad06d2`) shipped with wrong messages — content is correct,
labels are wrong. **Do NOT history-rewrite to fix this.**
Document corrections in handover instead. If you commit and
your commit appears under someone else's message, your code
still landed.

**2. Z-stacking in `/room` is now `z-[100] isolate`.** Don't
remove this — it was the fix for footer leaking through and
Settings drawer being occluded. Per `apps/web/app/room/layout.tsx`.

**3. `/po` lives outside the repo.** Located at
`~/.claude/skills/po/`. Not under git. If you want to share
with collaborators, package via
`~/.claude/skills/skill-creator/scripts/package_skill.py`.
Validator (`quick_validate.py`) requires `PYTHONIOENCODING=utf-8`
on Windows or it crashes on the cp1252 codepage when emojis
appear in print statements.

**4. Dirty working tree exists from autonomous pipeline.** Many
files modified that are NOT mine:
`.arcanea/config/repos.json`, `.github/workflows/*.yml`,
`apps/web/tests/e2e/open-library/*`, `package.json`,
`packages/{ai-core,content-api,database}/package.json`. Stage
specific files only — never `git add .`. Per
`feedback_mass_revert_protection`.

**5. Cached-belief validation is NON-NEGOTIABLE.** Memory is
authoritative for intent + strategy ONLY, never for current
state. Disk-first before any "X is shipped" claim. The /po
skill enforces this; apply it manually when not invoking /po.

**6. Verified-on-disk facts about the local intelligence
fabric (as of this session):**

- Runtime config dirs (file counts within 2 levels):
  `.claude` (695), `.arcanea` (101), `.codex` (27), `.gemini`
  (12), `.starlight` (7). `.opencode` is **missing** despite
  `opencode 1.14.21` CLI being installed.
- Sibling repo: `~/Starlight-Intelligence-System` exists.
- Substrate file presence: `agent.md` (5), `skill.md` (13),
  `memory.md` (2), `arc.md` (1), `nea.md` (1), `arcanea.md`
  (4), `starlight.md` (2), `sis.md` (2). `design.md`,
  `program.md`, `soul.md`, `human.md`, `arcanean.md` all 0.
- CLIs verified: claude 2.1.121, codex 0.123.0, gemini 0.38.1,
  kilocode 7.2.10, opencode 1.14.21, gh 2.88.1, jq 1.8.1,
  bun 1.3.11. **Missing**: rg, fd, fzf, tmux. Zellij ambiguous
  (see Section 1).
- Vercel production env: `OPENROUTER_API_KEY`,
  `GOOGLE_GENERATIVE_AI_API_KEY`, Supabase ×5,
  `BLOB_READ_WRITE_TOKEN`, `AI_GATEWAY_API_KEY`, `XAI_API_KEY`.
  No GROQ/OPENAI/ELEVENLABS yet.

## Next Actions (ordered)

1. **Verify Zellij install state.** Open fresh PowerShell or
   Git Bash. Run `zellij --version`. If found, also run
   `winget list Zellij` and `where.exe zellij`. If missing,
   either re-run `winget install Zellij.Zellij --location
   "C:\Tools\Zellij"` with explicit location, OR fall back to
   WSL Ubuntu: `wsl --start Ubuntu` then `cargo install
   --locked zellij` (needs Rust in WSL).

2. **Paste Prompt 1 (Local Command Center Phase 0+1) into a
   fresh Claude Code tab.** Source: this session's chat
   transcript above the handover. Wait for it to ship
   `apps/dashboard` at localhost:3001 with 6 empty tabs.
   Confirm `pnpm-workspace.yaml` was updated.

3. **Paste Prompt 2 (Phases 2-6) only after Prompt 1 reports
   Phase-1 verification green.** It builds substrate editor,
   terminal bridge, agent flow viz, multi-agent system tabs,
   orchestrator brain.

4. **When tomorrow's API keys land**, run:
   ```bash
   cd /c/Users/frank/Arcanea/apps/web
   echo -n 'gsk_...' | vercel env add GROQ_API_KEY production
   echo -n 'gsk_...' | vercel env add GROQ_API_KEY preview
   echo -n 'gsk_...' | vercel env add GROQ_API_KEY development
   vercel redeploy <latest-prod-url>
   ```
   Then re-test https://www.arcanea.ai/room/lumina — the
   error toast should NOT appear; voice should stream.

5. **Architecture spec** `STARLIGHT_RUNTIME_ADAPTER_PROTOCOL.md`
   — write only after Prompt 2 returns Phase-0 diagnostic
   matrix. The spec emerges from observation, not imagination.

## Files to Read First

- `~/.claude/skills/po/SKILL.md` — the new prompt-optimizer
  skill. Use it before drafting any prompt.
- `~/.claude/skills/po/references/domain-stacks.md` — full
  registry of slash commands × subagents × skills by domain.
- `~/.claude/skills/po/references/non-negotiables.md` —
  Frank's constraints. Apply universally.
- `apps/web/app/room/layout.tsx` — the z-stacking fix; do not
  regress.
- `apps/web/app/intelligence/` — the Lumina Constellation
  (5 files), reference for any future visualization work.
- `apps/web/lib/intelligence/agents.ts` — typed registry of 22
  agents (single source of truth).
- `planning-with-files/HANDOVER_2026-04-26_OVERNIGHT_INTELLIGENCE.md`
  — context on the overnight session that produced
  /intelligence.
- `CLAUDE.md` (root) — behavioral rules + 16GB RAM mandate.
- `apps/web/CLAUDE.md` — design system + voice room patterns.

## Repo Map

| Repo | Purpose | State |
|---|---|---|
| `arcanea-ai-app` (this repo, origin) | Production Next.js, Vercel-deployed | head `91c81d55`, dirty working tree (autonomous pipeline edits) |
| `arcanea` (oss) | OSS mirror, public | not touched this session |
| `Starlight-Intelligence-System` (~/) | SIS canonical repo | 34-file Starlight Substrate vault built earlier today |
| `arcanea-onchain` | Onchain workspace | own CLAUDE.md + .mcp.json, untouched this session |
| `~/.claude/skills/po/` | New `/po` skill | NOT in any git repo; lives user-global |

## Memory Relevance

These memory entries shape next-session decisions:

- `feedback_ship_means_ship` — every commit must verify live,
  not just compile
- `feedback_always_verify_live` — content + APIs + runtime logs
- `feedback_cached_belief_validation` — disk-first before any
  state claim
- `feedback_mass_revert_protection` — stage specific files
- `feedback_no_coauthor_contamination` — no Co-Authored-By tags
- `feedback_design_tier`, `feedback_design_taste` — UI bar
- `feedback_luminor_naming_depth` — Skyrim depth always
- `feedback_hz_identity` — Hz backend-only
- `feedback_noosphere_aesthetic` — 3D neural-net realism, not
  anime
- `project_world_graph_deployed` — 12 Supabase tables exist
- `project_sis_repo` — SIS canonical at ~/Starlight-Intelligence-System
- `project_lumina_orchestrator` — Lumina is default voice
- `MODEL_ROUTING_DISCIPLINE_2026-04-26.md` — task class →
  model mapping (APEX=Opus, SENIOR=Sonnet, MECHANICAL=Haiku)

## Outstanding Threads

- Zellij install verification + setup.kdl + setup.sh creation
  (deferred to Local-Command-Center prompt 1 Phase 0)
- World Graph + Supabase auth wiring in /room (deferred to
  Room build prompt 1)
- Multi-agent voice council (deferred — extension of /room
  build)
- Tool calls in voice loop (image/music/book gen via
  Replicate/Fal/Suno) — deferred to Room build prompt 1 Phase 3
- Live constellation pulse via Supabase Realtime (deferred —
  needs Local Command Center first to have a place to surface)

## One-Line Status for the Next Agent

`/po` is shipped, two prompts ready in transcript above,
Zellij install ambiguous, Local Command Center is the next
deliverable, paste prompts to fresh tabs in order — Phase 0
diagnostic first, code last.

— Shinkami signing off, 2026-04-28
