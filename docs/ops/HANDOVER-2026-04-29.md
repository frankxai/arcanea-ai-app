# Handover — 2026-04-29 (Storage Emergency + /pp Improvements)

## Situation

Frank's machine hit a storage emergency: **C: drive at 126 MB free / 476 GB** (WSL2 fails below 5 GB). Session resolved the immediate fire (recovered to 11 GB free), restored a deleted `/pp` slash command, and improved the `@arcanea/peak-performance` package with a disk hotspots probe. Three open questions left for next agent on (a) extending `/pp` further, (b) executing Phase 1 of WSL/Ubuntu cleanup, (c) both.

## What's Done

- **Disk recovered: 126 MB → 11 GB free**
  - npm cache cleared (4 GB)
  - pnpm store pruned 3× (282 files removed from `AppData\Local\pnpm\store\v3`)
  - Orphan `pnpm/store/v10` removed (3.85 GB) — verified safe via `pnpm store path`
  - Inactive `~/.pnpm-store` removed (1.98 GB)
  - VS Code cache, AppData\Temp older than 3 days, Recycle Bin emptied (~0.8 GB)
  - 4 sister-project node_modules removed: `arcanea-code` (2.31 GB), `arcanea-flow` (1.01 GB), `arcanea-orchestrator` (0.63 GB), `arcanea-opencode` (0.59 GB) — regen via `pnpm install` per dir
  - Stale worktree `design-evolution/node_modules` removed (1.14 GB)
  - OneDrive Files On-Demand triggered for ~42 GB nominal (HiDrive-space-8628.zip 23.8 GB cloud, CYBORG.psb 3.32 GB cloud, lunarDiffusion 2 GB cloud, ComfyUI 1.52 GB cloud, ~6 zip backups 1.5-1.9 GB each cloud) — eviction processed async over ~30 min after dehydration tag

- **`/pp` slash command restored**
  - File: `~/.claude/commands/pp.md` (new, committed `aaabeec` in `frankxai/claude-code-config`)
  - Calls `node packages/peak-performance/dist/cli.js audit`
  - **Important context:** `~/.claude/` IS a git repo (origin: `frankxai/claude-code-config`) but its last commit before this session was 2026-01-24 (3 months stale). 18+ uncommitted modified agents still pending — left untouched, Frank should review & commit himself.

- **`@arcanea/peak-performance` improved (committed `9e40cb7` in Arcanea repo)**
  - New file: `packages/peak-performance/src/core/disk-hotspots.ts` — runs only when `freeGB < 20`. Detects:
    - Top cache dirs by size (`Get-ChildItem` replaced by `robocopy /l /e /bytes` for 100× speed on millions-of-files trees)
    - Orphan pnpm stores (versions other than `pnpm store path`) — **with safety: refuses to flag any as orphan if active store can't be identified**
    - Top single files >500 MB in user home (with CLOUD/LOCAL flag from `RECALL_ON_DATA_ACCESS` attribute)
    - Sister-project node_modules within cwd
    - WSL2 ext4.vhdx size
  - Updated `src/core/audit.ts` — generates per-target recommendations with exact paths and safe fix commands
  - Updated `src/fixes/autofix.ts` — added `pnpm-store-prune`, `pnpm-cache-clean`, `vscode-cache-clean`, `recycle-bin-empty`, and composite `pp-clean-all-safe` recipe

- **WSL admin compact script written: `Arcanea/scripts/compact-wsl-admin.ps1`**
  - Tries `Optimize-VHD` first (Hyper-V), falls back to `diskpart`
  - Requires admin elevation (this session couldn't elevate — diskpart errored on permissions)
  - Frank to run: open admin PowerShell, execute the script

- **Ubuntu WSL inspected** — see Critical Context below.

## What's Not Done

- **WSL2 vhdx not compacted** — needs admin elevation. Script ready but unrun.
- **17 of 21 Claude Code instances still running** (per `/pp`, eating ~9.5 GB RAM, 95% used). Frank's CLAUDE.md says MAX 4-5. **Frank's hand only** — I won't kill user processes.
- **18+ modified files in `~/.claude/`** still uncommitted (agents, .gitignore, acos/state.json) — left for Frank to review.
- **`.npmrc` security issue:** `C:/Users/frank/.npmrc` contains an `_authToken=npm_...` in plaintext. **Frank needs to rotate this token** via `npm token revoke` + create new one + store outside `.npmrc` (e.g. via env var or 1Password). Did NOT echo the token in this handover for safety.
- **Three open questions Frank asked, never answered before /handover invoked:**
  1. (a) Extend `/pp` with Claude VM bundle probe + WSL distros probe + live Claude sessions probe
  2. (b) Execute Phase 1 cleanup: unregister zombie `podman-machine-default`, clean inside Ubuntu (.npm cache 2.7 GB, .chromium-browser-snapshots 646 MB, old .nvm versions ~1.5 GB recoverable, old experiment dirs `arcanea-vscode` 3.2 GB / `arcanea-claw` 413 MB / `arcanea-ai-clean` 384 MB — total ~5-8 GB inside Ubuntu)
  3. (c) Both
- **Compact-WSL script not tested end-to-end** — needs admin run to verify.

## Critical Context

### WSL2 Ubuntu — investigated, decision pending
- **Ubuntu 24.04.3 LTS** — 50 GB vhdx allocated, **46 GB used inside**
- **No code activity in last 7+ days** — only `/etc/hostname` newer than `/home/frankx/.motd_shown`
- pnpm/yarn point to Windows side via `/mnt/c` — Ubuntu doesn't have its own dev toolchain
- Top consumers: `.arcanea` 3.7G, `.local` 3.6G, `arcanea-vscode` 3.2G, `.npm` 2.7G, `.nvm` 2.3G, `.claude` 1.7G, `repos/` 1.1G, `.chromium-browser-snapshots` 646M
- All `repos/*` git clones (claude-flow, agents, arcanea-codex, arcanea-core, arcanea-intelligence-os, etc.) **also exist on Windows side** — duplicates
- **Verdict:** Not actively used, but contains mirror configs. **Don't fully delete** — clean inside + compact for ~15-33 GB recovery.

### Zombie podman distro
- `podman-machine-default` registered in WSL but its vhdx file at `C:\Users\frank\.local\share\containers\...\ext4.vhdx` is **MISSING** (`ERROR_PATH_NOT_FOUND`).
- Safe to unregister: `wsl --unregister podman-machine-default` — no data to lose.

### Claude Code VM bundle (the surprise consumer)
- `C:\Users\frank\AppData\Roaming\Claude\vm_bundles\claudevm.bundle\rootfs.vhdx` = 8.8 GB (was 9.99 GB during session)
- `sessiondata.vhdx` = 3.32 GB
- Total: **~12 GB** that scales with active Claude Code sessions
- 21 Claude instances were running during session — directly contributes to VM growth
- This is the actual sandbox VM hosting tool execution (where these very bash commands ran)
- **`/pp` does NOT yet detect this** — open task (Q-a above)

### pnpm store gotcha (do not delete pnpm/store/v3)
- pnpm 8.15.0 active, store at `C:\Users\frank\AppData\Local\pnpm\store\v3` (4.31 GB)
- **First version of `/pp` improvements wrongly flagged v3 as orphan** because `execFileSync('pnpm', ...)` failed on Windows `.cmd` shim. Fix: use `shell: true` + try `pnpm.cmd` first.
- The improved `/pp` now refuses to flag any store as orphan if active can't be identified (line 167 of disk-hotspots.ts: `if (!active) return [];`).

### Memory rules (project_repo_mapping)
- Production: `frankxai/arcanea-ai-app` (origin)
- OSS: `frankxai/arcanea`
- Records: music studio (DON'T push here)
- This session pushed nothing. Two new commits exist locally on Arcanea + 3 ahead in `~/.claude` repo.

### Frank's CLAUDE.md constraints
- 16 GB RAM, MAX 4-5 Claude Code instances
- Never run `pnpm dev` and `pnpm build` simultaneously
- WSL2 vhdx on C:\, keep C: above 5 GB or writes fail (was 126 MB at session start — actively failing)
- Stage specific files only, NEVER `git add .`

## Next Actions (ordered)

1. **Resolve Frank's open question** — pick (a), (b), or (c) and execute. (a) is pure code work, no system changes. (b) needs Frank's confirmation per action since some are destructive. (c) is the full sweep.

2. **Frank close excess Claude instances** (21 → 4-5) — biggest single RAM win, his hand only. Can't be done by agent.

3. **Run `Arcanea/scripts/compact-wsl-admin.ps1` from admin PowerShell** — recovers 10-25 GB if Frank doesn't first clean inside Ubuntu (smaller win); 15-33 GB total if he does. Frank's hand needed for elevation.

4. **Rotate `.npmrc` npm publish token** — `npm token revoke <id>` then create new one + store via env var or 1Password instead of plaintext `~/.npmrc`. Frank's hand.

5. **Commit `~/.claude/` pending changes** — `~/.claude/` is git-tracked at `frankxai/claude-code-config` but 3 months stale. 18+ modified agents accumulated. Frank reviews + commits to prevent future loss like the `/pp` deletion that triggered this whole investigation.

6. **(Optional) Init weekly cron for `/pp`** — auto-audit + push history file. Would prevent next storage emergency from going undetected for days.

## Files to Read First

- `packages/peak-performance/src/core/disk-hotspots.ts` — new probe, ~250 lines, the highest-leverage code in this session
- `packages/peak-performance/src/core/audit.ts` (lines 50-115) — see how hotspots feed recommendations
- `packages/peak-performance/src/fixes/autofix.ts` — fix recipe registry; if extending /pp probes, add corresponding fixes here
- `~/.claude/commands/pp.md` — the slash command shim (33 lines) that calls the CLI
- `Arcanea/scripts/compact-wsl-admin.ps1` — admin script ready to run for WSL2 vhdx compact
- This file (HANDOVER-2026-04-29.md) — top to bottom

## Repo Map

| Repo | Path | Purpose | State |
|---|---|---|---|
| Arcanea | `C:/Users/frank/Arcanea` | Main monorepo | 2 commits ahead (open-library scaffold + /pp hotspots improvements) |
| Claude Code config | `C:/Users/frank/.claude/` | Personal hooks/agents/skills/commands | 3 commits ahead, 18+ uncommitted (Frank's pending work) |
| peak-performance pkg | `Arcanea/packages/peak-performance/` | `/pp` audit CLI | Improved this session, build + audit verified |
| Ubuntu WSL2 | `\\wsl$\Ubuntu\home\frankx\` | 46 GB inside, 7+ days idle | Cleanup pending |
| podman-machine-default | (WSL distro) | Zombie | Unregister whenever |
| Claude VM bundle | `AppData\Roaming\Claude\vm_bundles\` | Sandbox VM (this session ran here) | 12 GB, scales with active sessions |

## Memory entries relevant to next agent

From `MEMORY.md` (already auto-loaded):
- `feedback_session_protocol` — read `.arcanea/MASTER_PLAN.md` before any work
- `feedback_ops_workflow` — max 2 worktrees, digest pattern, route by strength
- `project_pp_audit_*` — historical /pp scores (D/C+/C-/D trend was declining)
- `project_peak_performance_product` — package architecture (CLI + MCP + 10 gates)
- `feedback_ship_means_ship` — "put on website" = commit + push + deploy live
- `feedback_cached_belief_validation` — disk-first rule before citing memory
- `project_local_toolchain_2026_04_28` — Zellij/rg/fd/fzf installed, arndawg port, arc launches 4-pane cockpit

No new memories saved this session — the storage event was a one-off, not a recurring strategy decision. The lessons are encoded directly in `disk-hotspots.ts` source comments instead.

## Final state metrics

- C: drive: **11 GB free** / 476 GB (was 126 MB at session start)
- RAM: **95% used** / 16 GB total (still critical — 21 Claude instances)
- /pp score: **65 B-** declining (RAM is the holdout — disk side is recovering)
- Last commits:
  - Arcanea: `9e40cb7 feat(pp): disk hotspots probe + targeted recommendations`
  - .claude: `aaabeec fix(commands): restore /pp Peak Performance audit command`

## Tone for next agent

Frank explicitly said this session: *"i gave some autonomy but also constrain dont fuck things up"*. He's tired of multi-agent screwups (see `feedback_mass_revert_protection` memory: 4,517 files / 1.16M lines nuked by a botched revert in March). When in doubt: ask before destructive ops. When confident (cache cleanup, regenerable artifacts): proceed. NEVER push to remote without explicit go-ahead. NEVER `git add .` — stage specific files.
