---
date: 2026-04-25
session_window: 11:00–11:30 UTC (assessment session)
prior_handovers:
  - docs/ops/HANDOVER-2026-04-21-AO-UNIFICATION.md
  - docs/ops/HANDOVER-2026-04-21-VOICE-FIX-BACKLOG.md
gate: Source · Guardian: Shinkami
---

# Handover — 2026-04-25 (Voice v3 mid-sprint + CI billing block)

## Situation

Arcanea monorepo (`frankxai/arcanea-ai-app`) is mid-flight on **Voice v3.0 sprint** (Days 0–4 shipped 13:55 local; Days 5–7 remaining). Production site `www.arcanea.ai` is live. **GitHub Actions is fully blocked at the billing layer** — every push since at least 10:32 UTC today is failing instantly with *"recent account payments have failed or your spending limit needs to be increased."* The fix for the older code-level red checks (PR #64 era) is **fully implemented on disk but never staged** — 18 files of CI Recovery work sit dirty in the working tree. Production keeps shipping despite red CI. RAM is at 969 MB free / 16 GB — below the 2 GB safe threshold, so this session worked sequentially with no agent spawning.

## What's Done (this session window + parallel agent activity today)

**Shipped to main today (parallel agent, not by this session):**
- `d19449e7 feat(voice): v3.0 Days 0-4 — diagnosis, mic-permission gate, TTS greeting, multi-tenant, local agent`
  - Day 0 diagnosis (`planning-with-files/VOICE_3.0_DAY0_DIAGNOSIS_2026-04-25.md`)
  - Day 1 defensive fixes for 5 probe seams: `room-client.tsx` perms query, `primeMic()` callback, debug overlay; `voice-daemon.mjs` browser-binary logging
  - Day 2 TTS greeting on room mount: `/api/voice/greeting` (edge, OpenAI tts-1-hd, 30 min LRU), per-persona `greetingFor()` with tenant override
  - Day 3 multi-tenant scaffold
  - Day 4 local agent
- `1abb44be docs(voice/v3): research + sprint plan + master prompt`
  - 4 planning docs in `planning-with-files/VOICE_3.0_*` (RESEARCH, SPRINT_PLAN, GROUND_UP, DAY0_DIAGNOSIS)
  - Compose-don't-fork verdict; pulls Picovoice Porcupine, ElevenLabs UI Orb, LobeChat patterns
- `17f348ac fix(voice/daemon): singleton via shared user-data-dir + 60s cooldown + focus-existing`

**This session's contribution (assessment only — no commits):**
- Verified actual state of main vs the prior session's claims (PR #64 status, voice merge train, CI failure root cause)
- Identified GitHub Actions billing block as the real blocker (not code regression)
- Confirmed CI Recovery work is staged-but-uncommitted; the 17 ClawHub-staged skills are unpublished but ready

## What's Not Done

| Item | Why blocked / status |
|---|---|
| Commit the CI Recovery work (16 modified files) | Awaiting Frank sign-off; would push but Actions billing must clear first to verify |
| Fix GitHub Actions billing | Frank-only at github.com/settings/billing/spending_limit |
| Push 17 ported skills in `clawhub-staging/arcanea/` to ClawHub | Needs `npm login` (carryover ARC-76 from 2026-04-05) |
| Voice v3 Days 5–7 | Sprint plan exists; reads `planning-with-files/VOICE_3.0_SPRINT_PLAN_2026-04-25.md` for ticket list |
| Commit 6 untracked planning snapshots + 5 voice screenshots + new `.arcanea/skills/` files | Awaiting cleanup decision (commit vs gitignore) |
| 820 ESLint warnings in `apps/web` | Real debt, scheduled post-CI-green |
| Migrate deprecated Next 16 `middleware` convention | Tech debt before next major bump |
| Modernize `frankxai/arcanea-claw` repo (stalled since Apr 4) | Separate repo session |
| 3 modified `book/las-tierras-de-luz/chapters/` files | Unknown intent — verify with Frank before committing |

## Critical Context

**1. CI is billing-blocked, not code-broken.** Every workflow run today exits in 2 seconds with billing message. The CI Recovery doc (`planning-with-files/CI_RECOVERY_2026-04-23.md`) is correct — eslint pin to 9.39.4, ajv/brace-expansion override cleanup, workflow updates from deprecated `next lint` → `pnpm --dir apps/web run lint`, e2e stabilization. All passes locally per its verification block. **Don't try to "fix" CI by changing more code — fix billing first, then push the existing recovery commit.**

**2. PR #64 was merged with all primary checks red on 2026-04-24.** That set a precedent. Every voice PR (#65, #67, #69) merged the same way. Quality gate is currently a paper tiger.

**3. RAM discipline.** 969 MB free / 16 GB. Per CLAUDE.md, < 2 GB free = no agent spawning. Work sequentially. Recommend killing idle Claude instances before next session.

**4. The 17 staged ClawHub skills already exist on disk** at `clawhub-staging/arcanea/{capture, harvest, inbox-triage, prompt-*, ship-it, ship-public, vault-atlas, weekly-brief, repo-triage, daily-brief, counter-coach}/SKILL.md`. The porter script (`scripts/port-skill-to-clawhub.mjs`) was tested and works. They're one `clawhub skill publish` away from launch.

**5. `arcanea.ai` apex returned 000 once during testing — false alarm.** Real behavior: 307 → www.arcanea.ai → 200. Site is healthy.

**6. HEAD ref corruption from 2026-04-25 02:00 nightly report has self-resolved.** `git symbolic-ref HEAD` returns `refs/heads/main` cleanly. The 170-commit `frankx.ai-vercel-website` divergence on `rescue/reconciled-2026-04-20` (mentioned in `GIT_HEALTH_2026-04-25.md`) is in a sibling repo (`C:/Users/frank/FrankX/frankx.ai-vercel-website`), not this one — separate decision Frank still needs to make.

**7. Production is on `arcanea-ai-appx` Vercel project (Starlight Intelligence team).** Remote = `origin` = `arcanea-ai-app`. Don't push to `records` (music studio remote).

## Next Actions (ordered)

1. **Frank: clear GitHub Actions billing** — github.com/settings/billing/spending_limit (5 min, P0 unblocker for all subsequent work).
2. **Stage + commit CI Recovery work in 3 separated commits** (no `git add .` per CLAUDE.md):
   - Commit A: `fix(ci): pin eslint 9.39.4, replace deprecated next lint, ajv/brace-expansion overrides, e2e stabilization` — covers `.github/workflows/ci.yml`, `.github/workflows/quality-gate.yml`, `apps/web/eslint.config.mjs`, `apps/web/package.json`, `package.json`, `pnpm-lock.yaml`, `packages/ai-core/package.json`, `packages/content-api/package.json`, `packages/database/package.json`, all 6 `apps/web/tests/e2e/open-library/*.spec.ts`
   - Commit B: `docs(planning): Apr 23–25 ecosystem/CI/git-health snapshots` — 6 planning files + `.arcanea/screenshots/` + `.arcanea/ECOSYSTEM_CANON.md`
   - Commit C: triage `.arcanea/skills/` untracked files + `book/las-tierras-de-luz/chapters/` modifications with Frank — don't commit blind.
3. **Push and verify CI flips green** on the next push event after billing clears.
4. **Resume Voice v3 Days 5–7** per `planning-with-files/VOICE_3.0_SPRINT_PLAN_2026-04-25.md`. Read sprint plan first; Day 5–7 has explicit ticket list with files + verification criteria.
5. **Sidebar (parallel-safe): commit `clawhub-staging/`** in its own commit. When Frank confirms `npm login` is done, run `clawhub skill publish` against the `arcanea/*` namespace for the 17 skills.
6. **After CI green: open burn-down issues** for 820 ESLint warnings, grouped by category (React Hooks first per Codex's CI Recovery recommendation).
7. **Decide on `clawhub-staging/`**: commit or `.gitignore` — currently noisy in `git status`.

## Continuation Plan (per `also plan how to continue`)

### This week (Apr 25–28)

- **Sat 04-25 evening (today):** Frank fixes Actions billing. Next agent commits + pushes the 3-commit CI recovery train. Verifies main goes green. If npm login done, publishes 4 MCPs (`@arcanea/mcp-server`, `@arcanea/registry-mcp`, `@arcanea/memory-mcp`, `@arcanea/publishing-house-mcp`) and runs ClawHub publish for 17 staged skills.
- **Sun 04-26:** Voice v3 Days 5–7 execution. Day 5 = Picovoice Porcupine wake-word integration. Day 6 = ElevenLabs UI Orb visual polish replacing the bad neural-net viz. Day 7 = end-to-end test + room-loop QA + recording for Frank.
- **Mon 04-27:** Burn down React Hooks compiler warnings (highest-risk category of the 820). Open one PR per file or per family — keep PRs small.
- **Tue 04-28:** Migrate deprecated Next 16 `middleware` → `proxy` convention. Decide on `frankxai/frankx.ai-vercel-website` 170-commit `rescue/reconciled-2026-04-20` divergence (push to main vs open PR vs revert).

### Next week (Apr 29–May 5)

- Modernize `frankxai/arcanea-claw` (stalled since Apr 4) for current OpenClaw substrate. Publish as `arcanea/claw-media-engine` skill on ClawHub.
- Open Library v2: 3-tier publishing tier with Guardian ratings (per `project_open_library_publishing.md` memory).
- Author Studio: arcanea-author central intelligence with BYOK + rich editor + Guardian reviews.
- Decide on Arc/Nea on-chain Q1+Q2 work (held under "architecture freeze through Gate 0").

### Permanent guardrails (carry into every session)

- **Never merge with red primary CI checks** once billing is restored. Reverse the PR #64-era precedent.
- **Never `git add .`** — always file-by-file (per CLAUDE.md + 2026-03-11 mass-revert incident).
- **Always verify live, not just 200** — images, API endpoints, runtime logs (per `feedback_always_verify_live.md`).
- **Memory is NOT authoritative for current state** — read disk before claiming versions, paths, deploys (per Cached-Belief Validation Protocol in CLAUDE.md).
- **RAM check before agents** — `cat /proc/meminfo | grep MemFree`. If < 2 GB, work sequentially.

## Files to Read First

- `planning-with-files/VOICE_3.0_SPRINT_PLAN_2026-04-25.md` — Days 5–7 ticket list with files + verification
- `planning-with-files/VOICE_3.0_DAY0_DIAGNOSIS_2026-04-25.md` — Root-cause map for room voice loop
- `planning-with-files/CI_RECOVERY_2026-04-23.md` — Why the 16 modified files are the right fix
- `planning-with-files/GIT_HEALTH_2026-04-25.md` — Two severe issues (FrankX 170-commit drift, HEAD corruption — latter resolved)
- `CLAUDE.md` — Cached-belief validation, RAM rules, never-`git add .`
- `apps/web/CLAUDE.md` — Component patterns, content loader (if touching web)

## Repo Map

| Repo | Purpose | State |
|---|---|---|
| `frankxai/arcanea-ai-app` (this) | Production Arcanea Next.js app | Active. Site live. CI billing-blocked. 18 files dirty. |
| `frankxai/arcanea` | OSS mirror | Alive ★4 — sync wiki + lore when CI green |
| `frankxai/Starlight-Intelligence-System` | Memory substrate | Canonical v7.x ★3 — already on npm |
| `frankxai/oh-my-arcanea` | Harness overlay | ★0, needs polish + promote |
| `frankxai/arcanea-claw` | OpenClaw-compatible service | Stalled Apr 4 ★1 — modernize next week |
| `frankxai/arcanea-onchain` | On-chain workspace | 2 months stale — held until Arc/Nea Gate 0 unfreezes |
| `frankxai/frankx.ai-vercel-website` | Production frankx.ai site | **170 commits unpushed** on `rescue/reconciled-2026-04-20` — Frank decision needed |

## Memory Relevance

Memory is 2 days stale (per system reminder). Most relevant entries for next agent:

- `feedback_ship_means_ship.md` — "Put on website" = commit + push + deploying
- `feedback_dependabot_guardrails.md` — Never batch major bumps
- `feedback_mass_revert_protection.md` — 2026-03-11 incident: always check diff scope
- `feedback_no_coauthor_contamination.md` — Never add Co-Authored-By trailers
- `feedback_always_verify_live.md` — Verify images + API + logs every deploy
- `project_arc_nea_openclaw_session_2026_04_23.md` — Arc/Nea split locked, GenCreator pivot
- `project_noosphere_ground_truth_2026_04_21.md` — LuminaOrb/voice/room shipped; Cortex/Orchestrator vapor
- `project_pp_audit_2026_04_14.md` — Last PP audit, 50/D, RAM 92%, max 4–5 agents (re-check soon)

**Do NOT trust without verification:**
- Any version number, ship status, file path older than 24 h
- Any "X is done" claim — read git log first
