---
description: Arcanea Orchestra — daily pulse, scorecard, gate status, agent ranking, ceremony, challenge, herald, streak. Default subcommand is `pulse`.
---

Invoke the `arcanea-orchestra` skill via the Skill tool. The skill is the meta-orchestrator for Frank's life-domain dashboard, gate detection, agent trust, and reward ceremonies.

Subcommand (default `pulse` if empty): $ARGUMENTS
