---
description: Arcanea Orchestrator — multi-CLI routing brain. Routes tasks across claude/opencode/codex/gemini, runs plans and workflows, dispatches via Bash.
---

Invoke the `arco` skill via the Skill tool. The skill teaches you to use the `arcanea-orchestrator` (`arco`) terminal CLI from inside this session.

User args (if any): $ARGUMENTS
