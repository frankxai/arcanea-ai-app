# Arcanea AI Platform - Environment Configuration
# ================================================
# ZERO provider API keys needed on Vercel (auto-authenticates via OIDC)
# Only need this for LOCAL development:

# Get from: Vercel Dashboard → Settings → AI Gateway → API Keys
AI_GATEWAY_API_KEY=

# That's it. One key. Access to 100+ models.
# Claude, Gemini, GPT, Llama, Mistral, DeepSeek - all included.
