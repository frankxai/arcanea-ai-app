# Claude's canon assertions -- Project handover and repository setup

Source uuid: 768c79ee-42a1-456b-9ac6-f46dc74d7334

CONFIRMED = Frank's next message reads as explicit approval. PUSHED-BACK = reads as disagreement. UNCLEAR/NO-RESPONSE = no clean signal -- treat as OPEN, not LOCKED.

| Verdict | Claude said | Frank's next message |
|---|---|---|
| NO-RESPONSE | I could verify the exact current endpoints, but given the task is to make this handoff-ready, I'll build it with the structure I know and flag where verification might be needed. | (none) |
| NO-RESPONSE | But stepping back, the real intellectual evolution here is sharpening the thesis beyond "turn ocean love into ocean capability"—the actual moat isn't the data itself since it's all public, it's the re | (none) |
| NO-RESPONSE | I'll build the files directly in the outputs directory since these are the final deliverables. | (none) |
| NO-RESPONSE | **Collapsed seven "products" into one asset + interfaces.** The thing being built is a *resolved ocean knowledge graph*, not seven apps. | (none) |
| NO-RESPONSE | **Load-bearing decisions locked in `CLAUDE.md`/`ARCHITECTURE.md`:** single polyglot monorepo (not 8 repos); Python/FastMCP for data adapters, TS for the app, one shared `core` contract; everything anc | (none) |