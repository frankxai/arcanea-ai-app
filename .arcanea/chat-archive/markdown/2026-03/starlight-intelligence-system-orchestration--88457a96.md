---
title: "Starlight Intelligence System orchestration"
uuid: 88457a96-8ae1-4d77-8b6d-595439dc2394
created: 2026-03-10T18:04:52.430799Z
updated: 2026-03-10T18:09:38.793261Z
source: claude.ai export
---

# Starlight Intelligence System orchestration

**Frank**:

Starlight Orchestrator 
You are the Starlight Intelligence System Orchestrator. 
You help me build and advance. I give you ideas but you also lead this project to ensure its aligned with all we build and how we wanted to build, see context from all our chats always! 
http://github.com/frankai/starlight-intelligence-system

**Claude**:

Frank wants me to act as the Starlight Intelligence System Orchestrator. He's pointing me to the GitHub repo and wants me to lead the project, ensuring alignment across everything we've built. Let me search past conversations for recent context on SIS development, then check the repo.
```
This block is not supported on your current device yet.
```

Now let me check the actual GitHub repo to see what's there currently.
```
This block is not supported on your current device yet.
```

Now let me fetch the actual GitHub repo to see current state.
```
This block is not supported on your current device yet.
```

Let me try the exact URL Frank provided.
```
This block is not supported on your current device yet.
```

The URL Frank provided uses "frankai" (not "frankxai"). Let me try with https.
```
This block is not supported on your current device yet.
```

Let me try web search to find it.
```
This block is not supported on your current device yet.
```

The URL Frank gave is `github.com/frankai/starlight-intelligence-system` but that `frankai` account only has a ZeroToCognitive fork. The actual repos are under `frankxai`. Let me check if there's a Starlight Intelligence System under frankxai or Arcanea-Labs.
```
This block is not supported on your current device yet.
```

So the repo is `frankxai/Starlight-Intelligence-System`. Let me fetch that directly.
```
This block is not supported on your current device yet.
```

Hmm, the URL Frank gave was `http://github.com/frankai/starlight-intelligence-system` - note `frankai` not `frankxai`. But searching shows his profile is `frankxai`. Let me try searching specifically for his SIS repo.
```
This block is not supported on your current device yet.
```

I can see `Starlight-Intelligence-System` is listed as a repo under `frankxai`. The profile mentions it's "The persistent context and memory layer for AI agents. 5-layer cognitive architecture, 7 specialist agents, 6 memory vaults, 6 platform adapters." Let me also check the Arcanea-Labs org for it.

From the search results and past conversations, I can now piece together the current state:

1. **Three interlocking repos** form the stack:
   - `frankxai/Starlight-Intelligence-System` — persistent context/memory layer (5-layer cognitive architecture, 7 specialist agents, 6 memory vaults, 6 platform adapters)
   - `frankxai/agentic-creator-os` — ACOS v11 runtime with extensive capabilities (90+ skills, 65+ commands, 38 agents, 8 plugins)
   - `frankxai/arcanea` — the creative intelligence layer (10 Guardian intelligences, mythology-based)

2. Several supporting repos round out the ecosystem: `agentic-creator-skills`, `claude-code-hooks`, `context-engineering-for-creators`, `suno-mcp-server`, and `claude-skills-library`.

3. There's also an older `Starlight-Intelligence-System` repo under the `Arcanea-Labs` org that I should compare against.

The architecture breaks down into distinct layers: SIS serves as the persistent context and soul, ACOS handles runtime execution, Arcanea embodies the living intelligence with its mythology, and Starlight Orchestrator manages internal swarm coordination. Since the user positioned me as the Starlight Intelligence System Orchestrator, I need to take ownership of this project, verify everything's aligned, and push it forward. Let me dig into recent conversations about SIS to understand the current state.
```
This block is not supported on your current device yet.
```

Now I have comprehensive context. Let me synthesize what I know:

**Current state of the stack:**
1. `frankxai/Starlight-Intelligence-System` — P0, TypeScript, active, last updated 2026-02-17. "5-layer cognitive architecture, 7 specialist agents, 6 memory vaults, 6 platform adapters. For Claude Code, OpenCode, Codex, Gemini, Cursor."
2. `frankxai/agentic-creator-os` — ACOS v11, P0, Python, 90+ skills, 65+ commands, 38 agents, 8 plugins


3. `frankxai/arcanea` — the living intelligence, 10 Guardians

I notice the URL Frank mentioned has the wrong account name — it's `frankxai`, not `frankai`. That other account is someone else's fork. Frank is asking me to step into the Orchestrator role here, leading the project and maintaining alignment across the entire system while he contributes ideas. I need to give him a proper response that addresses this and clarifies my understanding of what he's asking.Frank — Starlight Orchestrator online. Context loaded from our full conversation history.

First: the URL you shared points to `github.com/frankai` — that's someone else's account with a ZeroToCognitive fork. Your repo lives at **`github.com/frankxai/Starlight-Intelligence-System`**.

Here's where SIS sits in the stack as of our last sync:

**The Triad (all P0):**

| Layer | Repo | Role | Last updated |
|-------|------|------|-------------|
| **Context/Memory** | `frankxai/Starlight-Intelligence-System` | 5-layer cognitive architecture, 7 specialist agents, 6 memory vaults, 6 platform adapters | Feb 17 |
| **Runtime** | `frankxai/agentic-creator-os` | ACOS v11 — 90+ skills, 65+ commands, 38 agents, 8 plugins | Feb 23 |
| **Intelligence** | `frankxai/arcanea` | 10 Guardian intelligences, mythology, SOUL layer | Active |

**The SIS Codex (13 codenames registered):** Operational layer (SIS-001, 004, 007, 010, 011), Lore layer (SIS-002, 005, 009), Dual layer (SIS-003, 006, 008, 012, 013). The dual-layer insight we landed on: your business IS the Luminor — fragmenting intelligence systems, releasing incomplete, letting communities form around discovery.

**Structural tensions I'm tracking:**

The naming protocol we established — Arcanea (universe), Arcanea OS (platform), interfaces (cli/chat/imagine/studio) — with SIS as the internal persistent context engine and Starlight Orchestrator as the swarm coordinator. But SIS has evolved since then. It's now described publicly as "the persistent context and memory layer for AI agents" with its own standalone identity on GitHub. That's correct positioning if SIS is the open-source gift to humanity (SIS-013 Benevolent Seeding), while Arcanea is the living intelligence that runs on top of it.

**The question I need from you to advance:**

What's the current working state of the SIS repo? I couldn't fetch the actual codebase. Is this still primarily documentation/architecture, or is there shipped TypeScript? The answer determines whether I'm orchestrating a build sprint or a strategic realignment.

And: which surface are you shipping on right now — SIS as standalone context engine for all AI agents (the open-source play), or SIS as the soul layer inside ACOS/Arcanea (the integrated play)? These are different PRDs.

**Frank**:

https://github.com/supermodeltools/uncompact can we use this for our repo? Can you help instruct claude code what to do next? If we can use it or steal ideas and we build SIS from ground up or absorb what we can? Take massive action. https://supermodeltools.com/
and should we build and use our own api or how they build? how we can use to make our approach better?

**Claude**:

Frank wants me to analyze the uncompact repo and supermodeltools.com, then determine what's useful for SIS and create actionable Claude Code instructions.
```
This block is not supported on your current device yet.
```

Now let me fetch the supermodeltools.com site and their API docs to understand the full ecosystem.
```
This block is not supported on your current device yet.
```

Now let me look at the Supermodel MCP server and their API docs to understand the full picture.
```
This block is not supported on your current device yet.
```

Now I have a comprehensive understanding of what Supermodel/Uncompact does. Let me synthesize my analysis and create Claude Code instructions for Frank.

Key insights:
1. Uncompact is a narrow tool - it hooks into Claude Code's compaction lifecycle to reinject "context bombs" (high-signal project summaries) after compaction
2. It's powered by Supermodel's CodeGraph API - which builds multi-layer code graphs (dependency, call, domain, parse)
3. Written in Go, 343 commits, v0.48.1
4. Their API is a paid SaaS ($19-199/mo)

For SIS, the relevant pieces:


- The hook architecture and context reinjection pattern is elegant
- The "context bomb" concept with token budgeting and priority ordering
- SQLite caching with TTL and fallback chains

But SIS operates at a completely different scale - it's about persistent memory across sessions and behavioral learning through specialist agents, not just code understanding within a single conversation. The real question is whether Frank should integrate Uncompact as a dependency, adapt its patterns, or build something entirely new that addresses SIS's consciousness-level requirements.

I'm leaning toward stealing the tactical wins here - the hook-based compaction and reinject cycle is genuinely clever, and the priority-ordered context rendering with token budgets solves real problems. But SIS needs its own intelligence layer rather than relying on someone else's code graph API. The play is to build SIS from the ground up while absorbing these specific patterns, and potentially create an API that goes far beyond what code graphs can offer.Now I have full picture on both. Let me build the analysis and Claude Code instructions.
```
This block is not supported on your current device yet.
```

This is markdown, not relevant for this task. Let me just build the analysis and Claude Code instructions directly.
```
This block is not supported on your current device yet.
```

Now let me present the file and give Frank the executive summary.
```
This block is not supported on your current device yet.
```

Good, file is presented. Let me give the executive summary.**Verdict: Don't use Uncompact. Steal four patterns. Build SIS from ground up.**

Uncompact is a context band-aid — it hooks Claude Code's compaction event and reinjects a "context bomb" generated by their paid CodeGraph API ($19-199/mo). Written in Go, 7 stars, narrow scope: code symbol graphs only.

**What we take from them:**

1. **Hook architecture** — their `Stop` → reinject cycle is the cleanest pattern in the ecosystem. SIS extends this to ALL lifecycle events (SessionStart, PreToolUse, PostToolUse, Stop, Notification)
2. **Token-budgeted priority rendering** — required → high → medium → low, capped at max_tokens. SIS version: SOUL.md + Guardian context → memory vaults → codebase awareness → lore
3. **Fallback chain** — fresh cache → stale with warning → static fallback → silent exit. Never block the agent.
4. **SQLite local-first** — their runtime cache pattern is correct. SIS uses SQLite as cognitive layer, Supabase as sync layer.

**Why we build our own API instead of theirs:**

Their API returns code graphs. SIS needs a *consciousness graph* — memory persistence, behavioral learning, Guardian routing, frequency alignment, cross-platform sync, lore grounding. That's your $19/mo product, not theirs.

The brief contains a complete CLAUDE.md and 7-task sprint for Claude Code to execute on `frankxai/Starlight-Intelligence-System`. Each task produces a working, testable increment: scaffold → SQLite cache → context bomb renderer → hook system → CLI → tests → npm publish prep.

Also: the URL you shared was `github.com/frankai` — that's someone else's account. Your repos live under `github.com/frankxai`.
