---
"@arcanea/mcp-server": patch
---

Add the read-only `search_sovereign_depths` tool to the existing CLI transports. It retrieves the public Arcanea boss/dungeon collection only with explicit proposal opt-in, preserves STAGING status, and rejects mismatched upstream collection envelopes.
