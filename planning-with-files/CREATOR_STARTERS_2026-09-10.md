# Creator starter integration

Source: Codex task `01a0881f-6721-7131-9dbb-3d191d52b7c6`; integration task `01a08b42-40aa-7211-a459-7ac8a72a78ae`. Frank requested better music, AI-lab and AI-tool templates, a simpler current Codex plugin architecture, and reviewed commits reconciled into each canonical main. Separate template repositories are included in the reconciliation. Earlier briefs and verification history remain in this file at commit `0a5a3230a` and the source task.

Owned lane: `codex/arcanea-commercial-truth-20260910`, PR #394. Pricing, checkout and shared-capture edits in the same worktree remain separate and unfinished. Foundation #397 merged as `6c0818797` after exact-head Gemini review and full CI. The continuation preserves main's SEO changes and resolves package add/add conflicts by retaining the nine-starter source.

Delivery: one catalog, one portable plugin identity, one skill, one dependency-free generator; nine original standalone examples, exact HTML downloads, adaptation briefs and namespaced registry bundles. The private workspace package makes the web generator dependency explicit without another published package or version authority. Web dev/build generate 39 ignored artifacts; Turbo tracks their sources and output. A small /templates layout provides discovery while preserving the blueprint page. A route-specific CSP permits the existing curated fonts while blocking network forms and connections for these standalone examples.

Acceptance: 15 Node 22 tests, compatible portable metadata, zero output drift, exact source/bundle bytes, full app build/type/lint, exact-head independent review and real browser evidence. Existing CI verifies the app link, 27 downloads, all nine desktop/phone pages, loaded fonts, synthesis, keyboard/clipboard flows, JSON recovery, reduced motion and no-script readability. No gate is disabled.

Evidence: package design-loop-evidence.json and CI gallery artifacts. Authenticated Chrome inspected Session, Margin, Patch and gallery interactions before a subsequent memory HOLD. The current head still needs fresh full CI and independent review. Automated captures are not a self-assigned premium score.

Boundaries: original examples, not published v0 templates. No v0 generation, marketplace submission, fresh-task plugin installation, paid checkout or commerce-release certification. Never promote the static-only preview over the full app. Roll back only the bounded package, generator, layout and integration changes; preserve the separate commerce work.

Formatter ownership: pnpm-lock.yaml retains pnpm's native generated format. Prettier excludes only that file; the unchanged Lockfile Drift Check and frozen dependency installs still validate it. Prettier's default rewrite produced over one million diff characters without changing dependency meaning. Authored-file formatting, dependency validation, security and browser checks remain required.
