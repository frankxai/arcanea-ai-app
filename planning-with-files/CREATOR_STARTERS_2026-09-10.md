# Creator starters — continuation contract

Source: Codex task `01a0881f-6721-7131-9dbb-3d191d52b7c6`, continued by Frank's request for better web design, music-producer templates, AI-lab and AI-tool landing pages, and simpler current Codex plugin architecture.

Scope: six original, downloadable, interactive website examples; a filterable gallery; tailored v0 briefs; one portable plugin with a generated Codex compatibility manifest. This is a free source resource inside the existing templates surface, not a newly priced product or a functioning business represented by its fictional examples.

Owner: current Codex task, sequential writer in the existing commercial-truth worktree. Earlier pricing, checkout and shared-capture changes remain separate and preserved. Machine preflight permits one bounded workload and pauses new swarms; design, engineering and verification roles run sequentially.

Files: `packages/arcanea-creator-starters/`, generated `apps/web/public/creator-starters/`, a link in `apps/web/app/templates/page.tsx`, this record.

Non-goals: marketplace publication, changing existing installed plugins, monetization, domains, production promotion, provider credentials, production research or model-inference claims.

Acceptance: six distinct briefs and usable previews; no dead demo CTAs, fabricated signup success, API keys or dependencies; keyboard and 375px support; content available without motion; output drift test; real downloads; manifest validation; desktop/mobile visual inspection; independent provider critique when admitted.

Scene brief: an editorial template library presents actual page compositions first. Music uses a record sleeve and sample instrument; lab pages use an experiment and model card; tools demonstrate a local transformation and inspectable workflow. The primary journey is choose audience → inspect preview → download source or copy v0 brief. Each sample names its demo boundary in the first viewport. Signatures differ by composition and interaction, not only palette. Motion responds to input; synthesized sound requires a play action.

Reference bar: v0's Pointer AI landing page and Auralink template for first-read craft; Vercel's v0 quickstart for editable/exportable workflow. Create original artwork and layout; no third-party template source is copied. Buyer hypothesis: a producer needs a listenable identity, a lab needs evidence and limitations, and a tool founder needs an understandable input/output demonstration. These are design hypotheses, not customer-research findings.

Verification: Node build and regression tests, compatibility and portable-schema checks, browser desktop/mobile/reduced-motion, existing commercial-truth checks. Preview-only deployment after local gates; production integration remains separately reviewable.

Rollback: remove the bounded template link and named new package/output files; preserve all commercial-truth work. No data migration or external state is involved in the templates themselves.

## Verification and delivery

- Refined preview: https://arcanea-ai-d35685zgz-starlight-intelligence.vercel.app
- Vercel project: `arcanea-ai-app`; deployment `dpl_8pkQ5KWgteGpmBQhDkYEBQgKPkML`; target is preview. The preview is the static template collection only; it is not a replacement deployment of the full app. Never promote this static-only deployment over the production application.
- Eleven Node 20.20.2 regression tests pass. Official Node executable SHA-256 was verified against nodejs.org before use. An earlier pnpm runtime attempt resolved to Node 24 and was not counted as the Node 20 gate.
- Root manifest passes the published Agent Plugins 1.0.0 JSON schema. The local Codex compatibility validator and skill validator pass; compatibility metadata is derived from the root manifest.
- All 12 deployed source/brief downloads return HTTP 200 and match local bytes by SHA-256. `.html.txt` download endpoints avoid hosting-injected preview scripts, and download attributes name the saved file `.html`.
- Every generated local link and anchor resolves. All seven HTML pages stay under a 100KB-per-page source budget. No dependency installation or full Next.js rebuild is needed for the standalone export.
- Desktop (1440px) and mobile (375px) visuals inspected in the Codex browser for all six designs and the gallery. The mobile thumbnail was refined after inspection. No horizontal overflow was observed in tested mobile layouts.
- Browser interactions: category filtering; synchronized play/stop on record and pad controls; research step switching; local plan/brief formatting; script-like input remains text; clipboard copy succeeds. Source-download API accepted both HTML and Markdown actions; the generic download-event waiter timed out in the in-app browser, so HTTP byte verification is the delivery evidence.
- Reduced motion sets native scroll behavior to auto. The gallery still shows all six examples with script execution disabled. Temporary emulation settings are reset before handoff. No local server was started.
- Browser error log contained a pre-existing Vercel login/FedCM error from the authentication page; no template-origin runtime error was observed.
- Changed Next.js template-link file passes scoped ESLint. The sparse worktree lacks the complete app dependency tree; ESLint emitted environment-discovery warnings. No full-app build or integration-release certification is claimed.
- The inherited commercial-truth verifier still passes its 65 mocked-storage assertions. Those inherited pricing, checkout and capture edits remain outside this template checkpoint.

Independent verifier verdict: **Unavailable / release hold**. The prior Claude review could not authenticate; current Grok and Gemini admission probes also report signed-out sessions. No independent PASS is claimed. This blocks production-release certification, not the reviewable source and preview delivery.

Residual gates: independent provider review, plugin installation in a fresh task, intentional distribution-license choice, full-app integration preview, and separate completion of the commercial-truth release work. v0 briefs and source are ready to import; no v0 session, marketplace template or public plugin submission was created.

Next bounded action: restore an independent reviewer session and review this exact package, then validate the template link in the full-app preview. Adapt the selected starter to a real owner-provided brief and evidence before considering a live product launch.

## v0.2.0 continuation — nine starters

Source: Frank continued the linked task on 2026-09-10, requesting stronger music-producer, AI-lab and AI-tool templates and simpler current Codex plugin architecture. Owner: current Codex task, sequential continuation of this worktree. The earlier six-starter checkpoint and audit remain preserved above.

Delivered candidate: Session (release listening room), Margin (research publication) and Patch (developer request workbench); category plus text search with shareable URLs and empty-state recovery; per-starter prompt copying; nine registry bundles containing exact standalone source and adaptation briefs; readable lab evidence tables replacing orbit illustrations; loaded Geist, Geist Mono and Instrument Serif fonts; restrained paper grain. Clipboard handling is shared across exports. Native JSON parsing reports syntax only, preserves invalid input and disables stale copying.

Architecture: one catalog and one portable plugin identity, one generator, a common page shell, distinct artifact renderers and a small registry adapter. The generated compatibility manifest remains derived from root plugin.json. No added package dependencies, server, database, provider or global MCP configuration. Registry imports have namespaced file targets; they do not install a React route or call a provider. No installed marketplace was edited.

Verification: 15 Node 20 regression tests pass; generated output has zero drift; portable plugin, Codex compatibility, skill and all nine registry bundles validate against their schemas. All local links resolve and each HTML file is under 100 KB. Gitleaks reports no leaks in the source package. The static anti-slop heuristic scores 90/100; that is not a visual-review score. Scoped template-link ESLint passes with sparse-worktree environment warnings. The inherited commercial-truth verifier still passes 65 mocked-storage assertions. New interactions are tested with lightweight event fixtures, not a full browser DOM. The registry/source comparison test exposed a whitespace mismatch; normalizing before bundling fixed it.

Candidate preview: https://arcanea-ai-f8o9615kh-starlight-intelligence.vercel.app — deployment dpl_Bj2FHxr6FNUVTEKCqmWr7n8NE3RT, confirmed READY through Vercel. Static export only, target preview, no production alias. Never promote this static-only deployment over the full application. The preceding deployment on the project was allowed to reach READY before this one was uploaded.

Access verification: unauthenticated HTTP requests return the Vercel login page rather than source bytes. The authenticated connector also returned the SSO redirect. Do not claim public download verification or use a shared bypass credential as a public registry URL. The local source/brief/bundle contents and archive CRC are verified. A signed-in user can inspect this preview; v0 can consume the local source and brief without relying on the protected URL.

Release decision: **candidate only; visual and independent-review gates remain open**. Latest Peak Performance browser preflight: HOLD, 5993 MB free versus 8192 MB required, 13 task runtimes versus budget 8. No new browser QA loop, full Next.js build, swarm or local server was started. Claude authentication was rechecked and is signed out. No independent reviewer PASS, visual score, fresh plugin install, v0 generation, marketplace publication or production-readiness claim is made. Evidence lives in packages/arcanea-creator-starters/design-loop-evidence.json. The earlier six-template visual inspection cannot certify these changes or the new font loading.

Next bounded action: after machine admission permits it, inspect all nine pages at desktop/mobile and run keyboard, font, contrast and reduced-motion QA; obtain independent provider critique; verify a signed-in v0 import and a fresh-task plugin install. Then integrate the full-app preview and consider release. Pricing, waitlist, billing and entitlement delivery remain separate unfinished work from the source task.

Rollback: revert only this template refinement checkpoint. Earlier commercial-truth files and customer data are unaffected. This continuation is a local checkpoint; draft PR #394 still represents the earlier pushed revision until an intentional later push.

## Integration continuation — 2026-09-10

Frank explicitly requested committing and reconciling the v0/template branches into their canonical main branches, including separate template repositories. The Arcanea lane remains `codex/arcanea-commercial-truth-20260910`, PR #394, based on `02ba41c5b`. The two starter commits are `90c5dbd61` and `ae0fd3d45`; no unique starter commits were found on another Arcanea branch. Pricing and commerce audit edits remain separately preserved in this worktree.

The earlier draft CI run 34429971972 passed ESLint, TypeScript and production compilation but failed generated-file formatting and a Sovereign Depths gallery interaction latency check. The integration replaces checked-in generated files with deterministic generation before web dev/build, preserves all nine examples and 39 outputs, adds cache input/output tracking and runs starter tests plus output checks in existing CI. Required checks are preserved. The obsolete generated copies remain recoverable in the two source commits.

The independent reviewer on protected main has a 50-file/120,000-character limit. Source-only delivery avoids sending duplicate generated HTML and JSON to the reviewer. Ready-for-review and merge require fresh checks and actual visual evidence; a successful static deployment alone does not prove either.

## Main reconciliation and complete-app gate

The independently reviewed source foundation merged through PR #397 as `6c0818797` on 2026-09-10. The source was split into this complete six-starter package and the nine-starter integration to fit the protected review limit; generated output is built rather than committed. Main's intervening SEO changes are preserved. Add/add conflicts were resolved only within the starter package by keeping its reviewed continuation to nine; no global ours strategy or force push was used.

The full-app CI now runs the 15 starter tests, verifies all 39 generated outputs and uses the existing Chromium/server lifetime to verify 27 download byte identities, twenty desktop/mobile page exports, Session synthesis, Margin keyboard/clipboard, Patch invalid-input/script-text/clipboard handling, gallery search recovery, reduced motion and no-script readability. Evidence is uploaded inside the existing gallery artifact. Fresh CI and exact-head review remain required for this integration; the foundation pass alone does not certify it.

Authenticated Chrome subsequently inspected Session, Margin and Patch at desktop and phone sizes plus the gallery. Its screenshots are retained in the private task evidence directory. No independent visual score, v0 marketplace publication, fresh-task plugin installation, or completed commerce release is claimed.
