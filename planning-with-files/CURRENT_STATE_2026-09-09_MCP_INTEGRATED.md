# Integrated modular MCP runtime candidate

Source: Codex task `01a086b7-f24a-7d40-98d3-f94d9f6a2ba9`.
Branch: `codex/arcanea-world-mcp-integrated-20260909`.
Parents: modular preparation `b0acd605966a25fcb3a4173b3585f56db19ac397`
and tested runtime/package/identity candidate
`168d24f2c6315883b21d48749fdd4cb8e024fbd9`.

Scope: Reconcile the verified runtime with the smaller registration modules.
Owner: capability-delivery coordinator, one writer. Files: the prior runtime and
package changes, generator/persistence registrations, and this record. No canon,
provider execution, library packaging, reviewer policy, global registration,
npm publication or production settings change.

## Resolution and verification

The factory creates a fresh MCP server and composes the existing registration
groups. Generator registrations use the collision-safe identity helper. World
save/load registrations retain the validated persistence and graph replacement
behavior. Session memory uses the tested runtime implementation. These resolve
the index and memory merge conflicts while preserving the modular structure.

All 65 registration-call syntax trees match the tested runtime candidate. Actual
SDK discovery, resource and prompt reads, six successful local tool calls, and
two rejected invalid requests remain byte-identical to its installed package:
91,928 bytes, SHA-256
`fc8eec5dc25ad20171003c224770386122bada2ed8cec04ed8d5c4891d07da76`.

All 395 source-layout tests pass, zero failures or skips. The release gate now
includes public-entrypoint composition tests; all 14 prepublication tests pass.
Prepack rebuilds and verifies all nine entrypoints. All changed files pass the
unchanged formatter. The source-layout fixture includes the verified repository
library and is not a claim that those assets are bundled.

The fresh archive contains 234 files and is 200,684 bytes. A new installation with
lifecycle scripts disabled matches every archive file. Its real installed CLI
preserves twelve creations at one timestamp, twelve nodes and 69 relationships
across process restart, and previously stored numeric identifiers. Every test
process closes. The prior identity archive remains byte-identical.

Archive SHA-256:
`b992ab190b19d6fd4887ad9e67e97132df205b5f1e5898a13c0f2ea50ee78357`.
Detailed receipts and machine-local artifacts remain in the private ledger.
Tracked generated files are restored after packaging and excluded from the diff.

## Integration and release

The preparation PRs must be integrated and the final candidate must pass current
CI and independent review. The earlier runtime and formatting approvals do not
approve this combined candidate. Preserve the public integration-route repair in
the promotion sequence; an unpublished archive does not repair production users.

Rollback: revert this integration while preserving the original parent candidates.
Existing world identifiers require no migration. No package has been published.
