---
description: Alias for /design-review. Runs verifier subagent against an existing page.
---

# /design-verify

See `/design-review`. This is an alias for teams that prefer "verify" terminology.

User arguments: `$ARGUMENTS` — forwarded to the verifier subagent via the same workflow as `/design-review`.
