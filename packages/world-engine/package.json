{
  "name": "@arcanea/world-engine",
  "version": "0.3.0",
  "description": "Pure worldbuilding logic shared between the Arcanea MCP server and web app",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test:ecology": "pnpm run build && node --test tests/ecology.test.mjs",
    "test": "pnpm run test:ecology"
  },
  "keywords": [
    "arcanea",
    "worldbuilding",
    "fantasy",
    "generators"
  ],
  "author": "FrankX",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
