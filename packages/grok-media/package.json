{
  "name": "@arcanea/grok-media",
  "version": "0.1.0",
  "private": true,
  "description": "Arcanea Grok Media Export & Library Manager — Chrome Extension",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "build:zip": "node build.mjs && node scripts/package.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "dexie": "^4.0.11",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "~5.5.4",
    "vitest": "^2.1.8"
  }
}
